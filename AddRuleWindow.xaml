<Window x:Class="BrowserSelector.AddRuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:BrowserSelector.Controls"
        x:Name="RuleEditorWindow"
        Title="Add New URL Rule" Height="480" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F3F3F3"
        ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LinkRouter;component/Themes/Windows11Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>      <!-- Scrollable content -->
            <RowDefinition Height="Auto"/>   <!-- Fixed buttons -->
        </Grid.RowDefinitions>

        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="24,20,24,0">
            <StackPanel>
                <!-- URL Pattern -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="URL Pattern"
                               Style="{StaticResource BodyStrongTextStyle}"
                               Margin="0,0,0,8"/>
                    <TextBox x:Name="PatternTextBox"
                             Style="{StaticResource TextBoxStyle}"
                             Height="36"/>
                    <TextBlock Text="Enter domain or partial URL (e.g., github.com, google, localhost)"
                               Style="{StaticResource CaptionTextStyle}"
                               Margin="0,6,0,0"/>
                </StackPanel>

                <!-- Browser/Profile Selection (reusable component) -->
                <Border Background="#FAFAFA"
                        BorderBrush="#E4E4E4"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="16"
                        Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="Browser &amp; Profile"
                                   Style="{StaticResource SubtitleTextStyle}"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Select which browser(s) to use when URLs match this pattern"
                                   Style="{StaticResource CaptionTextStyle}"
                                   Margin="0,0,0,12"/>
                        <controls:BrowserProfileMultiSelector x:Name="ProfileSelector"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Fixed Buttons at Bottom -->
        <Border Grid.Row="1"
                Background="#F3F3F3"
                BorderBrush="#E4E4E4"
                BorderThickness="0,1,0,0"
                Padding="24,16,24,24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SaveButton"
                        Content="Save"
                        Click="SaveButton_Click"
                        Style="{StaticResource SuccessButtonStyle}"
                        Width="100"
                        Margin="0,0,12,0"/>
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="100"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
