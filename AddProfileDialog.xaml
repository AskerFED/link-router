<Window x:Class="BrowserSelector.AddProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="DialogWindow"
        Title="Add Profile" Height="400" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="#F3F3F3"
        ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LinkRouter;component/Themes/Windows11Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="24,24,24,0">
            <StackPanel>
                <!-- Browser Selection -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Browser"
                               Style="{StaticResource BodyStrongTextStyle}"
                               Margin="0,0,0,8"/>
                    <ComboBox x:Name="BrowserComboBox"
                              Style="{StaticResource ComboBoxStyle}"
                              Height="36"
                              SelectionChanged="BrowserComboBox_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Grid Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <Image Source="{Binding Icon}" Width="16" Height="16"
                                               Visibility="{Binding Icon, Converter={StaticResource NullToCollapsedConverter}}"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                        <Ellipse Width="12" Height="12" Fill="{Binding Color}"
                                                 Visibility="{Binding Icon, Converter={StaticResource NullToVisibleConverter}}"/>
                                    </Grid>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!-- Profile Selection -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Profile"
                               Style="{StaticResource BodyStrongTextStyle}"
                               Margin="0,0,0,8"/>
                    <ComboBox x:Name="ProfileComboBox"
                              Style="{StaticResource ComboBoxStyle}"
                              Height="48">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Avatar Circle -->
                                    <Grid Width="24" Height="24" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <!-- Profile Image (when available) -->
                                        <Ellipse Width="24" Height="24"
                                                 Visibility="{Binding HasAvatar, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding Avatar}" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>

                                        <!-- Initials Fallback (when no avatar) - light gray -->
                                        <Grid Visibility="{Binding HasAvatar, Converter={StaticResource BoolToCollapsedConverter}}">
                                            <Ellipse Width="24" Height="24" Fill="#BDBDBD"/>
                                            <TextBlock Text="{Binding Initial}"
                                                       Foreground="White" FontWeight="SemiBold" FontSize="11"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Grid>

                                    <!-- Profile Info -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextStyle}"/>
                                        <!-- Email shown when available (collapses when empty) -->
                                        <TextBlock Text="{Binding Email}"
                                                   Style="{StaticResource CaptionTextStyle}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Visibility="{Binding Email, Converter={StaticResource StringNullOrEmptyToCollapsedConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!-- Custom Name -->
                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Custom Name (optional)"
                               Style="{StaticResource BodyStrongTextStyle}"
                               Margin="0,0,0,8"/>
                    <TextBox x:Name="CustomNameTextBox"
                             Style="{StaticResource TextBoxStyle}"
                             Height="36"/>
                    <TextBlock Text="Displayed in picker instead of the default name"
                               Style="{StaticResource CaptionTextStyle}"
                               Margin="0,6,0,0"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons (fixed at bottom) -->
        <Border Grid.Row="1"
                Background="#F3F3F3"
                BorderBrush="#E4E4E4"
                BorderThickness="0,1,0,0"
                Padding="24,16,24,24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ActionButton"
                        Content="Add"
                        Click="AddButton_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="90"
                        Margin="0,0,12,0"/>
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="90"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
